env:
  frame_stack: 4
  skip_frames: 2

# Training
training:
  max_episodes: 900
  target_score: 750
  convergence_episodes: 100
  convergence_threshold: 10
  save_frequency: 100
  eval_frequency: 100
  eval_episodes: 10

# Logging
logging:
  log_dir: "logs"
  use_tensorboard: true

# Checkpoints
checkpoints:
  save_dir: "checkpoints"

# Random seed
seed: 42

# Device
device: "cpu" # or "cuda"